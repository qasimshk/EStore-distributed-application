namespace estore.api.tests.Units.Validation;

using estore.api.tests.Fixtures;

public class Class1
{
    [Fact]
    public void TestOne()
    {
        //var customers = DataFixture.GetCustomer().Generate(1);

        //var orders = DataFixture.GetOrders(string.Empty).Generate(1);

        //customers.Should().NotBeEmpty();

        //orders.Should().NotBeEmpty();

        Assert.True(true);
    }
}
